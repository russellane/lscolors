usage: lscolors docs [-h] [-f] [DIR]

This application's packaging process uses this internal command to create this application's documentation.

positional arguments:
  DIR          create directory `DIR`. (default: './docs')

optional arguments:
  -h, --help   show this help message and exit
  -f, --force  Ok to clobber `DIR` if it exists
